version: '3.8'

services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped
    # GPU 사용 시 아래 주석 해제
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]

  # AnythingLLM (선택사항 - 웹 UI 원할 경우)
  # anythingllm:
  #   image: mintplexlabs/anythingllm:latest
  #   container_name: anythingllm
  #   ports:
  #     - "3001:3001"
  #   volumes:
  #     - anythingllm_data:/app/server/storage
  #   environment:
  #     - STORAGE_DIR=/app/server/storage
  #   depends_on:
  #     - ollama
  #   restart: unless-stopped

volumes:
  ollama_data:
  # anythingllm_data:
